#include <bits/stdc++.h>
using namespace std;

int main() {
    string x;
    cin >> x;

    int n;
    cin >> n;

    string s[n];
    for(int i=0; i<n;i++){
        cin >> s[i];
    }

    //　任意の辞書順をどのように比較するか

    return 0;
}
